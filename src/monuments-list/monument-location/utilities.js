const toggleLocationDisplay = {
    displayMonumentLocation: (lat, lon) => alert('display location' + lat + '/' +  lon),
    hideMonumentLocation: () => alert('hiding location')
}

export default toggleLocationDisplay